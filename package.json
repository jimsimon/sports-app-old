{
  "name": "sports-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "migrate:latest": "lerna run migrate:latest --scope api",
    "migrate:make": "lerna run migrate:make --scope api",
    "migrate:rollback": "lerna run migrate:rollback --scope api",
    "seed:make": "lerna run seed:make --scope api",
    "seed:run": "lerna run seed:run --scope api",
    "start:api": "lerna run start --stream --scope api"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.4.1"
  }
}
